package knbit.emm.algo.graph.edge.state;

/**
 * This nested enum represents three possible states of the edge (more precisely exchange which edge relates to)
 */
public enum State {
    ACCEPTED, PENDING, DECLINED
}
